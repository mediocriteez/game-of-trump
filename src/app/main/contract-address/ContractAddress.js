"use client"

import {useEffect} from 'react'
const WalletAddress = ({setStartAction, setCurrentContent, position}) => {

    useEffect(() => {
        
    }, [])
    
    return(
        <div style={{background: 'green'}} data-position={position}>
            wallet address
        </div>
    )
}

export default WalletAddress