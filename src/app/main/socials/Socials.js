"use client"

import {useEffect} from 'react'
const Socials = ({setStartAction, setCurrentContent, position}) => {

    useEffect(() => {
        
    }, [])
    
    return(
        <div style={{background: 'red'}} data-position={position}>
            socials
        </div>
    )
}

export default Socials