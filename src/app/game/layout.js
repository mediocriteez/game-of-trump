export const metadata = {
    appleWebApp: {
        capable: 'yes',
        statusBarStyle: 'black-translucent'
    }
}

const layout = ({children}) => {
    return (
        <>
            {children}
        </>
    )
}

export default layout